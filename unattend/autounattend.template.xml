<?xml version="1.0" encoding="utf-8"?>
<!--
  Answer file for an OEM machine that has NEVER been through OOBE.

  Windows Setup searches removable media (precedence 4) at the beginning of every
  configuration pass, so this is picked up at the oobeSystem pass on a factory
  machine - no reinstall or reimage is needed. Put it at the ROOT of the stick.

  Double-brace placeholders are filled in by make-usb.sh. This template contains
  no secrets and is safe to commit; the generated file is NOT.
-->
<unattend xmlns="urn:schemas-microsoft-com:unattend">

  <settings pass="specialize">
    <component name="Microsoft-Windows-Shell-Setup" processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS"
               xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">
      <!-- Also passed to the bootstrap below, because some OEM images are sysprepped
           /oobe without /generalize, in which case this pass never runs. -->
      <ComputerName>{{COMPUTER_NAME}}</ComputerName>
      <TimeZone>{{TIMEZONE}}</TimeZone>
    </component>
  </settings>

  <settings pass="oobeSystem">
    <component name="Microsoft-Windows-International-Core" processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS"
               xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">
      <InputLocale>en-US</InputLocale>
      <SystemLocale>en-US</SystemLocale>
      <UILanguage>en-US</UILanguage>
      <UserLocale>en-US</UserLocale>
    </component>

    <component name="Microsoft-Windows-Shell-Setup" processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS"
               xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">

      <OOBE>
        <HideEULAPage>true</HideEULAPage>
        <HideOEMRegistrationScreen>true</HideOEMRegistrationScreen>
        <HideOnlineAccountScreens>true</HideOnlineAccountScreens>
        <HideLocalAccountScreen>true</HideLocalAccountScreen>
        <HideWirelessSetupInOOBE>true</HideWirelessSetupInOOBE>
        <NetworkLocation>Work</NetworkLocation>
        <ProtectYourPC>3</ProtectYourPC>
      </OOBE>

      <!-- The break-glass account, created before first logon. -->
      <UserAccounts>
        <LocalAccounts>
          <LocalAccount wcm:action="add">
            <Name>{{ADMIN_USER}}</Name>
            <DisplayName>{{ADMIN_USER}}</DisplayName>
            <Group>Administrators</Group>
            <Password>
              <Value>{{ADMIN_PASSWORD}}</Value>
              <PlainText>true</PlainText>
            </Password>
          </LocalAccount>
        </LocalAccounts>
      </UserAccounts>

      <!-- One automatic logon so FirstLogonCommands can run unattended; reverts after. -->
      <AutoLogon>
        <Enabled>true</Enabled>
        <LogonCount>1</LogonCount>
        <Username>{{ADMIN_USER}}</Username>
        <Password>
          <Value>{{ADMIN_PASSWORD}}</Value>
          <PlainText>true</PlainText>
        </Password>
      </AutoLogon>

      <FirstLogonCommands>
        <SynchronousCommand wcm:action="add">
          <Order>1</Order>
          <Description>Fleet unattended setup</Description>
          <!-- Locates its own USB by marker file, since the drive letter varies. -->
          <CommandLine>powershell.exe -ExecutionPolicy Bypass -NoProfile -Command "foreach($d in 'DEFGHIJKLMNOPQRSTUVWXYZ'.ToCharArray()){$p=\"${d}:\fleet\setup.ps1\"; if(Test-Path $p){&amp; $p; break}}"</CommandLine>
        </SynchronousCommand>
      </FirstLogonCommands>

    </component>
  </settings>
</unattend>
